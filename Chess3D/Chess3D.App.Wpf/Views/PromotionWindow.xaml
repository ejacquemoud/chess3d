<Window x:Class="Chess3D.App.Wpf.Views.PromotionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Promotion"
        Width="470"
        Height="308"
        MinWidth="470"
        MinHeight="308"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#0B1016"
        Foreground="#D7E0EA"
        FontFamily="Segoe UI Variable Display, Segoe UI, Arial"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <Window.Resources>
        <SolidColorBrush x:Key="WindowBgBrush" Color="#0B1016" />
        <SolidColorBrush x:Key="PanelBrush" Color="#111C28" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#213142" />
        <SolidColorBrush x:Key="CardBrush" Color="#132131" />
        <SolidColorBrush x:Key="CardHoverBrush" Color="#18283A" />
        <SolidColorBrush x:Key="CardPressedBrush" Color="#1C2D40" />
        <SolidColorBrush x:Key="CardBorderBrush" Color="#294057" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#D7E0EA" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#A8B4C2" />
        <SolidColorBrush x:Key="AccentBrush" Color="#8EC1FF" />

        <Style x:Key="TitleTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style x:Key="SubtitleTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style x:Key="PieceGlyphStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontSize" Value="30" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="PieceLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="6,6,6,8" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinHeight" Value="112" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="8,10,8,10" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource CardHoverBrush}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource CardPressedBrush}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>

                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBgBrush}">
        <Border Margin="16"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="18,18,18,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20"
                                  ShadowDepth="0"
                                  Opacity="0.20"
                                  Color="#000000" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="6" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Choisir la pièce de promotion"
                           Style="{StaticResource TitleTextStyle}" />

                <TextBlock Grid.Row="1"
                           Text="Sélectionnez la pièce qui remplacera votre pion :"
                           Style="{StaticResource SubtitleTextStyle}"
                           Margin="0,8,0,16" />

                <UniformGrid Grid.Row="2"
                             Rows="1"
                             Columns="4"
                             Margin="0,2,0,4"
                             VerticalAlignment="Center">

                    <Button Tag="Queen" Click="PromotionButton_Click">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="♕" Style="{StaticResource PieceGlyphStyle}" />
                            <TextBlock Text="Dame" Style="{StaticResource PieceLabelStyle}" />
                        </StackPanel>
                    </Button>

                    <Button Tag="Rook" Click="PromotionButton_Click">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="♖" Style="{StaticResource PieceGlyphStyle}" />
                            <TextBlock Text="Tour" Style="{StaticResource PieceLabelStyle}" />
                        </StackPanel>
                    </Button>

                    <Button Tag="Bishop" Click="PromotionButton_Click">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="♗" Style="{StaticResource PieceGlyphStyle}" />
                            <TextBlock Text="Fou" Style="{StaticResource PieceLabelStyle}" />
                        </StackPanel>
                    </Button>

                    <Button Tag="Knight" Click="PromotionButton_Click">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="♘" Style="{StaticResource PieceGlyphStyle}" />
                            <TextBlock Text="Cavalier" Style="{StaticResource PieceLabelStyle}" />
                        </StackPanel>
                    </Button>

                </UniformGrid>
            </Grid>
        </Border>
    </Grid>
</Window>